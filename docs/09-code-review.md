# Code Review v1.0 (второй проход перед релизом)

Дата: 2026-09-10. Полный проход по всему коду (`src/`, `data/`, `public/`, конфиги).
Формат: находка → статус. Все пункты из «Исправлено» входят в коммит v1.0.

## Критические (эксплойты / безопасность)

| # | Находка | Статус |
|---|---|---|
| 1 | **Pump-and-dump эксплойт**: FARM поднимал хайп (+0.5/долю) → цена росла → мгновенный SELL давал безрисковые ~+2.25/клик; при крупной позиции покупка 5 долей поднимала стоимость всех удерживаемых на ~40% себестоимости | ✅ Исправлено: покупка не двигает цену (регресс-тест) |
| 2 | **XSS через лидерборд**: `publicName` других игроков вставлялся через `innerHTML` на экране результатов | ✅ Исправлено: `esc()`-экранирование (юнит-тест) |
| 3 | **Дейлик-эксплойт**: старт дейлики → выход = +75 монет + стрик + «сожжённая» попытка | ✅ Исправлено: выход не платит и не сжигает (smoke-тест) |
| 4 | `seasonsPlayed++` при выходе из сезона → накрутка анлоков контент-сезонов выходами | ✅ Исправлено: только доигранные сезоны |
| 5 | Неверное имя метода SDK `leaderboards.setLeaderboardScore` (по документации — `leaderboards.setScore`) → проверка доступности всегда проходила мимо | ✅ Исправлено |
| 6 | Лидерборд перезаписывался худшим результатом (setScore перезаписывает значение) | ✅ Исправлено: сабмит только при новом личном рекорде |

## Игровые баги

| # | Находка | Статус |
|---|---|---|
| 7 | Спарклайны перерисовывались каждый кадр (60×/сек × 5 карточек): `number !== string` всегда true | ✅ Исправлено (+перф на слабых телефонах) |
| 8 | Rewarded из паузы: симуляция тикала под рекламой (потерянные секунды сезона), музыка играла | ✅ Исправлено: пауза держится до закрытия ад-колбэка |
| 9 | Музыка не глушалась перед rewarded/interstitial (требование модерации Яндекса) | ✅ Исправлено: хуки before/after в AdsService |
| 10 | `game_api_pause/resume` от SDK не обрабатывались | ✅ Исправлено: подписка в YandexPlatform, музыка глушится |
| 11 | Состояние `PAUSED` существовало в SM, но не использовалось (boolean рядом) | ✅ Исправлено: проведено через SM, переход PAUSED→RESULTS добавлен |
| 12 | Кнопка «Ещё раз» в дейлике вела в меню (ложь в UI) | ✅ Исправлено: для дейлики — одна кнопка «В меню» |
| 13 | x2-кнопка предлагала посмотреть рекламу за награду 0 (после выхода) | ✅ Исправлено: скрыта при нулевой награде/выходе |
| 14 | SW cache-first: после обновления игры игроки могли застрять на старом билде | ✅ Исправлено: network-first + версия кэша v2 |
| 15 | `fmt()` всегда ru-формат чисел даже в EN | ✅ Исправлено: локале-зависимо |
| 16 | Счётчик ачивок в меню — хардкод «13» | ✅ Исправлено: считается по ACHIEVEMENTS |
| 17 | Тост ключа `season2_unlocked` — не масштабируется на сезон 3+ | ✅ Исправлено: `season_unlocked` |
| 18 | Апгрейд «Кофе-машина» ур.1 (150 монет) не давал ничего | ✅ Исправлено: каждый уровень полезен |
| 19 | После удаления накачки и расширения пула до 26 мемов бот-гард падал до 16–19/40 | ✅ Исправлено: driftK 0.25→0.31, гард 24/40 |

## Чистота кода

| # | Находка | Статус |
|---|---|---|
| 20 | Мёртвый код: `musicNodes`, пустой блок red-tracking в tickSeason, `(energyLv >= 1 ? 0 : 0)` | ✅ Удалено |
| 21 | Magic numbers: комиссия, коэффициент дрейфа | ✅ В CONFIG (`sellFeePct`, `driftK`) |
| 22 | Неиспользуемая dev-зависимость playwright (тяжёлый install) | ✅ Удалена |
| 23 | `void profit;` и `void platform;` заглушки | Оставлены: сознательное гашение неиспользуемых параметров строгого tsc |
| 24 | Неиспользуемые i18n-ключи (auth_login, daily_title и др.) | Оставлены: задел под v1.1 (экран входа/описание дейлики), вреда нет |

## Что проверено и признано корректным (без изменений)
- Детерминизм сида и совпадение текста новости с целью эффекта (rollNews/applyNews).
- Отсутствие циклических импортов (граф модулей ацикличен; GameScreen не знает Game).
- Миграции сейвов v0→v2, бэкап-слот, конфликт cloud/local по прогрессу.
- Ежедневный сид по серверному времени Яндекса (анти-накрутка дейлики).
- Отсутствие секретов в клиенте; монетизация только через Yandex SDK (нет сторонней рекламы).
- Pools/weighted/shuffle — чистые функции, покрыты тестами.
